# Books-Classifier

Use XGBoost to classify books by reading level.

**Structure:**
- `src/preprocessing/` — book feature extraction and labeled data preparation
- `src/models/` — XGBoost classifier
- `src/training/` — training feature engineering
- `src/conf/` — configuration and parameters
